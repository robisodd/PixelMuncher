#pragma once
#include "global.h"
// =========================================================================================================== //
typedef struct SpectreStruct {
  struct XYStruct speed;
  struct XYStruct pos;
  
} SpectreStruct;
SpectreStruct spectre[4];
// =========================================================================================================== //
